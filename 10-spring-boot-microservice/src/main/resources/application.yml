server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:contasdb
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
